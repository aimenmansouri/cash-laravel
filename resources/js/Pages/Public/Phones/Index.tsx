import Guest from "@/Layouts/GuestLayout";

export default function Index({ employees = [] }) {
    return (
        <Guest>
            <div className="  text-cash-red">phones page</div>
        </Guest>
    );
}
